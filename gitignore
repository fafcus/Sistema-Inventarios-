# === Python ===
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/
env/

# === Bases de datos locales (ya no se usa, pero por las dudas) ===
*.db
*.sqlite
*.sqlite3
inventario.db

# === Visual Studio ===
.vs/
*.slnx.user

# === PyInstaller (build de la app) ===
build/
dist/
*.spec

# === Documentos reales del inventario (datos sensibles, no van a git) ===
documentos/

# === Archivos temporales de Word ===
~$*.docx

# === Scripts de diagnóstico / uso personal (no forman parte de la app) ===
diagnostico_inventario.py

# === Carpetas viejas sueltas que no son parte del repo ===
PythonApplication1/

# === Entorno / configuración local ===
.env
